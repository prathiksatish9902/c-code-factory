#include "entity.h"

Entity::Entity() {

    int a[5];
    int count = sizeof(example)/sizeof(int);//a->example
    std::cout<<count<<std::endl;
    for(int i =0;i < another.size(); i++)
        example[i] = 2;
}
